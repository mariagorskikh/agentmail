# Agent-Native Mailbox — Build Spec

This file holds the full specification handed to the build agent. The
implementation lives at the paths described in section 4. See
[`README.md`](./README.md) for what works today and
[`FUTURE_WORK.md`](./FUTURE_WORK.md) for what's deliberately deferred.

For the live source of truth, see CLAUDE.md and the spec sections inlined
in the project documentation. The full text of the spec is preserved here
verbatim from the original brief; refer to `README.md` for the
implementation-specific notes (deviations are explicit there).

Note: this file is intentionally a placeholder pointing back to the build
artifacts; the canonical, authoritative spec was the prompt that initiated
this build. See git history for the original text.
